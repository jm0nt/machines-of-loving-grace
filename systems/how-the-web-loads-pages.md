# How the Web Loads Pages

A web page is not a single file.

When a browser loads a page it may request many resources:

- HTML documents
- images
- stylesheets
- JavaScript files

Each resource is retrieved separately and combined by the browser
to render the final page.