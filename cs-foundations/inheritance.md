# Inheritance

Some systems allow properties to be inherited from parent structures.

In CSS, styles applied to a parent element can automatically
propagate to child elements.

Inheritance reduces repetition and helps maintain consistent
structure across complex documents.