# Markup Languages

HTML is a markup language rather than a programming language.

Programming languages describe procedures and logic.
Markup languages describe the structure of a document.

A browser reads HTML and interprets the structure
to render a page visually.