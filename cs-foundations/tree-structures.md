# Tree Structures

HTML documents are structured as trees.

Elements contain other elements, forming a hierarchy.
This structure is called the Document Object Model (DOM).

Understanding tree structures is fundamental in computer science
because many systems organize information hierarchically.