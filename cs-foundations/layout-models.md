# Layout Models

User interfaces often rely on layout models
that describe how elements occupy space.

The CSS box model treats each element as a
rectangular container with margins, borders,
padding, and content.

This abstraction allows browsers to render
complex page layouts.