# Rule Systems

Many computational systems operate through layered rules.

CSS uses cascading rules to determine how elements should
be styled when multiple rules apply.

More specific rules override more general ones.