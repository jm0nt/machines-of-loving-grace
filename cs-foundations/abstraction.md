# Abstraction

Abstraction allows developers to describe groups of things
with a single concept.

CSS classes are a simple example:
a style can be defined once and applied to many elements.

This reduces repetition and simplifies complex systems.