# Flow

Many systems arrange elements according to a flow.

In web pages, elements normally follow a vertical document flow.

Layout tools like floats modify that flow to position elements
in different ways.