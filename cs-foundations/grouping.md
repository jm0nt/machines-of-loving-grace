# Grouping

Complex systems often organize elements into groups.

HTML uses containers like `div` to group related elements.

Grouping allows developers to apply behavior or styling
to entire sections of a document.