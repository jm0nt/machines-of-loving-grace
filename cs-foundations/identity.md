# Identity

In many systems individual elements must be uniquely identifiable.

HTML IDs provide a way to uniquely identify elements
within a document.

Unique identifiers are used throughout computing,
from database keys to memory addresses.