## First HTML Page

Created a simple HTML document using a heading and paragraph.

HTML is not a programming language; it is a markup system
that describes the structure of a document so that a browser
can render it.